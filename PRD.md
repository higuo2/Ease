# 减脂追踪 App: Ease (个人自用版 v1.0)

## 1. 产品愿景与边界 (Vision & Boundaries)
* **愿景**：客观记录，聚焦趋势。提供一个安静、无压力且具极简现代质感的数据看板。
* **平台**：iOS 18+，仅 Light Mode。单位固定为 kg / cm，数值精度体重 0.1 kg、体脂 0.1%、围度若后续加入则为 0.1 cm。
* **Non-Goals (绝对禁止)**：
    * 禁止计算卡路里、三大营养素（碳水/蛋白质/脂肪）。活动消耗仅作为 HealthKit 客观读数展示，不据此打分或给饮食建议。
    * 禁止连续打卡火焰 (Streaks)、庆祝动效、目标达成彩蛋等制造情绪波动的机制。
    * 禁止 BMI 的 WHO 色档或「偏瘦 / 正常 / 超重」文案；BMI 只显示数字。
    * 禁止任何社交、社区分享或第三方账号登录。
    * 禁止应用内语言切换。
* **v1.1（本期不做）**：身体围度、桌面 Widget、体重+体脂双轴图、月历饮食视图、自定义变量标签、Dark Mode。

## 2. 信息架构与页面流转 (Information Architecture)
采用单页面无 Tab 栏架构，最大限度减少层级干扰。全 App 仅三种界面：Dashboard、录入 Sheet、设置 Sheet；另加首次启动的 Onboarding。

### 2.1 主控台 (Dashboard View) — 自上而下固定顺序
1. **导航**：标题 `Ease`；右上角设置入口。
2. **进度环**：紫色厚描边圆环。文案为已减 kg 与剩余 kg。目标达成后停在 100%，顶部改为冷淡事实 `Target 55.0 kg`，无庆祝动效。低于目标时不继续加圈；若当前体重大于初始体重，进度为 0%。
3. **今日状态条**：一行客观信息。无对应权限或无数据的项直接隐藏，不显示占位「—」。
    * 饮食：已选则显示对应 SF Symbol；未选则显示待打卡态。
    * 昨晚睡眠：如 `7.5 h`（无 HealthKit 睡眠权限则隐藏）。
    * 今日活动：如 `420 kcal`（无活动消耗权限则隐藏）。
    * 当日变量标签：有则显示对应 icon。
4. **主卡**：当前体重（大数字）。有体脂则在次要行显示。BMI 纯数字，例如 `BMI 21.4`，不做评估语。当前体重优先取 7 日均线；均线不足时取最新一条有体重的记录。
5. **图表卡**：标题行提供 `7 / 30 / 90` 天分段控件（只改 X 轴可见范围，均线窗口始终为 7 日）。体重用平滑折线；单日点视觉弱化；7 日均线高亮。变量标签以单色 SF Symbol 附着于对应日期节点。图表下方一排近 7 日饮食 icon 流水。
6. **HealthKit 辅助层**：与体重图共用 X 轴，以极简淡灰柱/辅助线展示当日 Active Energy（kcal）与前夜睡眠时长。对应权限未开则整段不渲染。
7. **FAB**：底部圆形 `+`，避开 Home Indicator。点击录入**今天**。

图表交互：按住/拖动预览该日体重、均线、饮食、标签；单击已有数据点打开编辑表单。无数据的日期不能靠「点空图」补录，须走表单内日期选择。

### 2.2 录入表单 (Log Sheet)
半屏 Modal。内部自上而下：

1. 日期选择（默认今天；不可选未来）。选已有记录日 = 编辑；选空白日 = 补录。
2. 体重（大数字输入）+ 行内「相册识图」入口。
3. 体脂（可选；识图成功可预填）。
4. 饮食三选一：Clean / Normal / Cheat。
5. 变量标签多选：经期 / 差旅 / 排空。
6. 备注（可选）。
7. 底部黑色 Capsule `Save`。编辑已有记录时另给弱化 `Delete`（删除该日整条）。

识图不另开页面。至少填写体重或饮食其中一项才允许保存。

### 2.3 极简设置 (Settings Sheet)
右上角入口。可改：身高、初始体重、目标体重、通知总开关、导出 CSV、清除全部数据。改初始/目标后进度环立刻重算。不提供语言切换、单位切换、自定义标签。

### 2.4 启航 (Onboarding)
2～3 步，不把权限和数据挤在一屏：

1. 原则说明（客观记录、无卡路里、无社交）。
2. 录入身高 (cm)、当前体重、目标体重。保存时**写入当天 `DailyRecord`（含体重）**，并将该体重同时存为初始体重，避免首页有进度、图表却为空。
3. 可选授权 HealthKit 与通知（可跳过；之后不反复弹窗）。相册权限延迟到用户第一次点识图时再申请。

## 3. 数据模型与业务规则 (Data Rules - SwiftData)
* **BMI**：`BMI = weight(kg) / height(m)^2`，仅用于主卡数字展示。
* **一天一条**：每个日历日有且仅有一条 `DailyRecord`。
* **字段级 Upsert（禁止整单覆盖）**：同一天再次保存时，只更新用户在本次表单中改过的字段；未改的字段保持原值。例如早上只记体重、晚上只改饮食，体重不得被清空或覆盖。
* **可空字段**：`weight` 与 `dietStatus` 均为可选。允许「只打饮食」或「只记体重」的记录。
* **进度环公式**：`progress = (startWeight - displayWeight) / (startWeight - targetWeight)`，clamp 到 `0...1`。`displayWeight` 优先 7 日均线，否则最新有效体重。
* **7 日均线**：窗口固定 7 天。不足 7 个有体重的数据点时：只画已有散点与折线，**不画均线**。
* **核心字段 (`DailyRecord`)**：
    * `date`: Date（按本地日历日唯一，忽略时分秒）
    * `weight`: Double?（精度 0.1 kg）
    * `bodyFat`: Double?（精度 0.1%）
    * `dietStatus`: Enum?（Clean / Normal / Cheat，每日至多一个）
    * `tags`: [String]（经期 `drop.fill` / 差旅 `airplane` / 排空 `wind`，可多选，不可自定义）
    * `note`: String?
    * `updatedAt`: Date（CloudKit 冲突时取较新者）
* **删除**：删的是该日整条记录。设置中的「清除全部数据」会删除全部 `DailyRecord` 并回到 Onboarding；身高/目标等用户设置一并重置。

## 4. 离线 AI 识图与异常降级 (OCR & Fallbacks)
* **定位**：快捷输入辅助，纯本地 Vision，不强制走识图即可完成录入。
* **流程**：相册选图 → 离线识别数字 → 预填体重与体脂 → **用户确认后点 Save 才落库**。
* **异常阈值**：体重须在 30–150 kg，体脂须在 5–50%。超出范围、解析失败、或一张图无法唯一确定数值，均视为失败。
* **降级**：失败时对应输入框留空，回退手动输入，不弹破坏性错误框。

## 5. HealthKit 静默同步与去重 (HealthKit Integration)
* **权限**：仅 Read-only。拒绝后静默隐藏关联模块，不反复申请。Onboarding 可跳过；首次进入 Dashboard 若仍未决定，不再自动弹。
* **经期去重**：HealthKit 显示当天经期时，与手动 `drop.fill` 合并，图表节点只画一个 `drop.fill`。
* **睡眠**：使用 `asleep` 时长，不用 in-bed。跨午夜间归属为「昨晚」。`< 6h` 才触发提醒追加句。
* **活动消耗**：Active Energy，单位 kcal，与图表共用 X 轴。无权限则今日条与图下辅助层都不渲染。

## 6. 状态感知提醒机制 (Context-Aware Notifications)
本地通知（`UNUserNotificationCenter`），不是远程推送。设置里一个总开关；关闭则调度全部取消。

* **分项防打扰（体重与饮食互不影响）**：
    * 08:00 `今日体重待记录。` — 仅当当天 `weight == nil` 时发送。
    * 22:30 `今日饮食状态待打卡。` — 仅当当天 `dietStatus == nil` 时发送。
* **客观事实联动**（符合条件时追加在对应那一条后面，不另发一条）：
    * 生理期：`经期数据已同步。水分滞留可能引发正常体重波动。`
    * 睡眠不足（昨晚 asleep < 6h）：`昨晚睡眠不足 6 小时。皮质醇升高可能影响数据表现。`

## 7. 国际化、同步与数据安全 (Localization, Sync & Security)
* **多语言**：简体中文与英文；`String Catalog` (.xcstrings)；跟随系统；无应用内切换。
* **隐私**：无第三方服务器、无 Analytics。SwiftData 本地为主；CloudKit 仅同步到用户个人 iCloud。无网时本地照常读写，联网后静默合。
* **冲突**：同一 `date` 两条记录，保留 `updatedAt` 较新者。
* **CSV 导出列**：`date, weight, bodyFat, dietStatus, tags, note`。
